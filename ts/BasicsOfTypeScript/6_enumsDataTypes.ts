enum UserRole {
  ADMIN = "admin",
  GUEST = "guest",
}

let role: UserRole = UserRole.GUEST;

console.log(role);

enum Size {
  Small = 1,
  Medium = 2,
  Large = 3,
}

let size: Size = Size.Large;

console.log(size);  // 3
